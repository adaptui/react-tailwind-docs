import {
  Callout,
  ComponentLinks,
  PropsTable,
  InteractiveCodeblock,
  Codeblock,
} from "@/components/index";

# Badge

Display an indicator that requires attention.

<ComponentLinks github="badge" themeColor="badge" story="primitives-badge" />

## Imports

```js
import { Badge } from "@renderlesskit/react-tailwind";
```

## Usage

<InteractiveCodeblock
  children={({ spreadProps }) => `<Badge${spreadProps}>Click me</Badge>`}
  themeProps={{
    size: "badge.size.common",
    variant: "badge.variant",
    themeColor: "badge.variant.solid",
  }}
/>

## Badge variants

Badge has 4 variants `solid` `subtle` `outline`

<Codeblock
  live
  children={`
<div className="space-x-2">
  <Badge variant="solid">Solid</Badge>
  <Badge variant="subtle">Subtle</Badge>
  <Badge variant="outline">Outline</Badge>
</div>
`}
/>

## Badge Sizes

Badge comes with 3 sizes `sm` `md` `lg`

<Codeblock
  live
  children={`
<div className="space-x-2">
  <Badge size="sm">Small</Badge>
  <Badge size="md">Medium</Badge>
  <Badge size="lg">Large</Badge>
</div>
`}
/>

## Badge Themes

Badge comes with 5 themes `default` `primary` `secondary` `success` `danger`

<Codeblock
  live
  children={`
<div className="flex flex-col space-y-1">
  <div className="space-x-2">
    <Badge size="md" variant="solid" themeColor="default">
      Beta
    </Badge>
    <Badge size="md" variant="subtle" themeColor="default">
      Beta
    </Badge>
    <Badge size="md" variant="outline" themeColor="default">
      Beta
    </Badge>
  </div>
  <div className="space-x-2">
    <Badge size="md" variant="solid" themeColor="primary">
      Beta
    </Badge>
    <Badge size="md" variant="subtle" themeColor="primary">
      Beta
    </Badge>
    <Badge size="md" variant="outline" themeColor="primary">
      Beta
    </Badge>
  </div>
  <div className="space-x-2">
    <Badge size="md" variant="solid" themeColor="secondary">
      Beta
    </Badge>
    <Badge size="md" variant="subtle" themeColor="secondary">
      Beta
    </Badge>
    <Badge size="md" variant="outline" themeColor="secondary">
      Beta
    </Badge>
  </div>
  <div className="space-x-2">
    <Badge size="md" variant="solid" themeColor="success">
      Beta
    </Badge>
    <Badge size="md" variant="subtle" themeColor="success">
      Beta
    </Badge>
    <Badge size="md" variant="outline" themeColor="success">
      Beta
    </Badge>
  </div>
  <div className="space-x-2">
    <Badge size="md" variant="solid" themeColor="danger">
      Beta
    </Badge>
    <Badge size="md" variant="subtle" themeColor="danger">
      Beta
    </Badge>
    <Badge size="md" variant="outline" themeColor="danger">
      Beta
    </Badge>
  </div>
</div>
`}
/>

<Callout>
  You can add extra variants, themeColor & sizes via the theme file. Checkout
  [theming guide](/docs/theming).
</Callout>

## Badge prefix

You can pass prefix props to the badge to prepend any content inside of badge.

<Codeblock
  live
  children={`
<div className="space-x-2">
  <Badge prefix={<SlotIcon />}>Search</Badge>
</div>
`}
/>

## API Reference

<PropsTable
  data={[
    {
      name: "size",
      default: "md",
      themeKey: "badge.size.common",
      description: "Size of the badge",
    },
    {
      name: "variant",
      default: "solid",
      themeKey: "badge.variant",
      description: "Variant of the badge",
    },
    {
      name: "themeColor",
      default: "default",
      themeKey: "badge.variant.solid",
      description: "Theme of the badge",
    },
    {
      name: "prefix",
      type: "React.ReactNode",
      description:
        "If added, the badge will show an element before the badge's text",
    },
  ]}
/>
